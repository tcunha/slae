#!/bin/sh

test $# -ne 1 && {
	echo "usage: ${0##*/} ip" >&2
	exit 1
}

echo -n 0x
echo $1|tr . '\n'|tac|while read n; do
	printf "%02x" $n|grep -q 00 && {
		echo "\rHost contains NUL bytes!" >&2
		exit 1
	}
	printf "%02x" $n
done
